<?xml version="1.0" encoding="UTF-8"?>
<!-- Oak Tree Asset Definition
     Procedural tree using shared Lua generator.
     Script path uses @shared/ prefix for shared scripts folder.
-->
<AssetDefinitions>
  <AssetDef>
    <defName>Flora_TreeOak</defName>
    <label>Oak Tree</label>
    <assetType>procedural</assetType>

    <generator>
      <!-- Shared script using @shared/ prefix -->
      <scriptPath>@shared/deciduous.lua</scriptPath>
      <params>
        <!-- Trunk dimensions in meters (oak is larger than maple, scaled for top-down) -->
        <trunkHeight>1.5</trunkHeight>
        <trunkWidth>0.2</trunkWidth>
        <!-- Canopy radius in meters -->
        <canopyRadius>0.8</canopyRadius>
        <!-- Number of visible branches -->
        <branchCount>4</branchCount>
      </params>
    </generator>

    <rendering>
      <complexity>complex</complexity>
      <tier>batched</tier>
    </rendering>

    <!-- Trunk-base collision comes from the generator (asset:setCollisionRect,
         half-extent = trunkWidth/2 = 0.1m), captured eagerly at load. -->

    <!-- Entity capabilities - felled for wood with an axe in one destructive action -->
    <!-- Whole-tree yield 20-30 wood (the old pool total); the colonist takes a weight-limited -->
    <!-- armful and the remainder drops as a loose, haulable ground pile. durability 150 ~= 15s. -->
    <capabilities>
      <harvestable yield="Wood" amountMin="20" amountMax="30" durability="150"
                   destructive="true" requiresTool="Axe"/>
    </capabilities>

    <placement>
      <biome name="TemperateDeciduousForest">
        <spawnChance>0.45</spawnChance>
        <distribution>spaced</distribution>
        <spacing>
          <minDistance>3.0</minDistance>
        </spacing>
      </biome>

      <!-- Groups this asset belongs to -->
      <groups>
        <group>trees</group>
        <group>deciduous_trees</group>
        <group>oak_trees</group>
        <group>large_flora</group>
        <group>wild_plants</group>
        <group>wood_sources</group>
      </groups>

      <!-- Relationships with other entities -->
      <relationships>
        <!-- minDistance (spaced) owns oak-to-oak spacing now; keep only a mild
             cross-tree avoid so a mixed canopy intermixes without overlapping trunks. -->
        <avoids group="trees" distance="2.5" penalty="0.7"/>
      </relationships>
    </placement>

    <variantCount>15</variantCount>
  </AssetDef>
</AssetDefinitions>
